name: easeopsdemo

build:
  image: megaease/nightwatch-demo
  script: release/build-image.sh

deploy:
  - environment: easeopsdemo
    script: release/deploy.sh

notification:
  emails:
    - g7tianyi@easestack.com
